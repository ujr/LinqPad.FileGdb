# Geometry

Here are low-level geometry operations that work on `double` values
or arrays of `double` values and are ignorant of any particular shape
representation.

Sources for geometry algorithms:

- The comp.graphics.algorithms FAQ:
  <http://www.faqs.org/faqs/graphics/algorithms-faq/>
